import React, {useState} from "react";

const EventCard = ({
  id,
  title,
  description,
  voting_finished,
  AdminId,
}) => {
  
  return (
    <div className="voting-ended-container">
    </div>
  )
};

export default EventCard;